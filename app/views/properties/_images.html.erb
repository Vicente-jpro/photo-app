<div id="<%= dom_id property %>">
  <p>
    <strong>Name:</strong>
    <%= property.name %>
    
    <div class="img">
    <% if property.images.attached? %>
      <% property.images.each do |image| %>
        <%= image_tag(image) %>
      <% end %>
    <% end %>
    <hr>
    </div>
  </p>

</div>
